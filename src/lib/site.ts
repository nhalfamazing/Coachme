// Canonical site origin, shared by metadata, sitemap, robots, and
// structured data. NEXT_PUBLIC_SITE_URL overrides in each environment.
export const SITE_URL =
  process.env.NEXT_PUBLIC_SITE_URL || "https://koachme.ai";
